<% provide(:title, 'All users') %>
<div class="page-title"><h1>All users</h1></div>

<%= will_paginate %>

<ul class="users">
  <%= render @users, current_user: current_user %>
</ul>

<%= will_paginate %>